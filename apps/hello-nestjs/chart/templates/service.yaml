apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-nestjs.fullname" . }}
  labels:
    {{- include "hello-nestjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "hello-nestjs.labels" . | nindent 4 }}
