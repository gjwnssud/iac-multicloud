apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-nestjs-local-libvirt
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/gjwnssud/iac-multicloud.git
    targetRevision: main
    path: apps/hello-nestjs/chart
    helm:
      valueFiles:
        - values.yaml
        - values-local-libvirt.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: hello-nestjs
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
